#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
//int Add(int x, int y)
//{
//	return x + y;
//}
//int main()
//{
//	int arr[10] = { 0 };
//	int (*p)[10] = &arr;//数组指针
//
//	int (*pfArr[4])(int, int);//函数指针数组
//	int (*(*ppfArr)[4])(int, int) = &pfArr;//ppfArr是一个数组指针，指针指向的数组有4个元素
//	//(*ppfArr)[4]指向的数组的每个元素类型是一个函数指针int (*)(int, int)
//	
//
//
//
//	//int(*parr2)[10];//去掉(*parr2)[10]，剩int
//	//parr2是一个指针，指针指向的数组有10个元素,每个元素的类型是int,parr2是数组的指针
//
//	//int (*(*ppfArr)[4])(int, int)//去掉(*ppfArr)[4]，剩int (*)(int, int) -- 可以理解成int (*)(int, int) (*ppfArr)[4]
//	//ppfArr是一个指针,指针指向的数组有4个元素,每个元素的类型是int (*)(int, int)，ppfArr是函数指针数组指针
//	
//	return 0;
//}

//int (*pf)(int,int);函数指针
//int (*pfArr[4])(int,int);函数指针数组

//int (*(*ppfArr)[4])(int, int);函数指针数组指针
//int (*(*ppfArr[4])[4])(int, int);函数指针数组指针的数组

